info:
  name: post
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "login": "newUSee",
        "name": "newUSee",
        "password": "newUSee"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
