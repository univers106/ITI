info:
  name: change_password
  type: http
  seq: 4

http:
  method: PATCH
  url: localhost:8080/api/auth/change-password
  body:
    type: multipart-form
    data:
      - name: old_password
        type: text
        value: password
      - name: new_password
        type: text
        value: password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
