info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/api/auth/login
  body:
    type: json
    data: |-
      {
        "login": "login",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
