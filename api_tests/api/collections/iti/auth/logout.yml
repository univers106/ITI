info:
  name: logout
  type: http
  seq: 3

http:
  method: POST
  url: localhost:8080/api/auth/logout
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
