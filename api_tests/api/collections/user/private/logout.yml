info:
  name: logout
  type: http
  seq: 2

http:
  method: GET
  url: localhost:8080/api/private/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
