info:
  name: hello
  type: http
  seq: 1

http:
  method: GET
  url: localhost:8080/api/private/hello
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
