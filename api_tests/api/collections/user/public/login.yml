info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/api/public/login
  body:
    type: multipart-form
    data:
      - name: login
        type: text
        value: sudo
      - name: password
        type: text
        value: sudo
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
